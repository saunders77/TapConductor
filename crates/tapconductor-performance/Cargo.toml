[package]
name = "tapconductor-performance"
version = "1.3.0"
edition = "2021"
rust-version = "1.75"
license-file.workspace = true
description = "Deterministic score cursor and gesture gate state machine for TapConductor"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
